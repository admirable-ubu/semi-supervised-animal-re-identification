The code cannot work as a stand-alone library. It needs the data files for the five videos (bounding boxes and constraints). 
They are named as in the repository at https://doi.org/10.5281/zenodo.7322820 and should ne stored in a local folder 
“../features/features”.
